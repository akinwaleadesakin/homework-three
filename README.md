# homework-three

## Installation

To install all dependencies, run:

```bash
npm install
```

## Available Commands

- **`npm run serve`** - Starts a live-server to preview your project in the browser
- **`npm run compile:sass`** - Compiles SASS files from `scss/styles.scss` to `css/styles.css` and watches for changes
